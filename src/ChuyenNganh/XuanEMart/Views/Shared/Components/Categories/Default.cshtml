@model IEnumerable<XuanEmart.Models.CategoryModel>

@foreach (var category in Model)
{
    <li>
        <a href="@Url.Action("Index", "Category", new { Slug = category.Slug }) ">@category.Name</a>
       
    </li>
}
